import './header.css';
import FeedbackInfo from '../feedbackInfo';

const Header = () => {
  return (
    <header className="header">
      <FeedbackInfo />
    </header>
  );
};

export default Header;
